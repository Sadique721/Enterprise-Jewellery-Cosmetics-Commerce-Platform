<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ═══════════════════════════════════════════════════════════════════
         SANAB — Enterprise Jewellery & Cosmetics Commerce Platform
         Organization: Antigravity Technology
         Architecture: Modular Monolith | Spring Boot 4.1 | Java 25
         ═══════════════════════════════════════════════════════════════════ -->

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath/>
    </parent>

    <groupId>com.antigravity.sanab</groupId>
    <artifactId>sanab-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>SANAB Backend :: Parent</name>
    <description>
        SANAB Enterprise Jewellery and Cosmetics Commerce Platform — Parent POM.
        Manages all module dependencies, plugin configurations, and version properties.
    </description>

    <!-- ═══════════════════════════════════════════════════
         MODULE DECLARATIONS (Dependency-ordered)
         ═══════════════════════════════════════════════════ -->
    <modules>
        <module>sanab-shared</module>
        <module>sanab-security</module>
        <module>sanab-identity</module>
        <module>sanab-notifications</module>
        <module>sanab-catalog</module>
        <module>sanab-inventory</module>
        <module>sanab-customer</module>
        <module>sanab-cart</module>
        <module>sanab-orders</module>
        <module>sanab-payments</module>
        <module>sanab-shipping</module>
        <module>sanab-promotions</module>
        <module>sanab-reviews</module>
        <module>sanab-search</module>
        <module>sanab-cms</module>
        <module>sanab-support</module>
        <module>sanab-analytics</module>
        <module>sanab-app</module>
    </modules>

    <!-- ═══════════════════════════════════════════════════
         VERSION PROPERTIES (Single source of truth)
         ═══════════════════════════════════════════════════ -->
    <properties>
        <!-- Java -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Spring Ecosystem -->
        <spring-modulith.version>1.3.1</spring-modulith.version>


        <!-- Persistence -->
        <flyway.version>10.22.0</flyway.version>

        <!-- Security -->
        <jjwt.version>0.12.6</jjwt.version>
        <argon2.version>2.11</argon2.version>

        <!-- Mapping -->
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok.version>1.18.38</lombok.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>

        <!-- Communication Providers -->
        <twilio.version>10.5.1</twilio.version>

        <!-- Utilities -->
        <commons-lang3.version>3.17.0</commons-lang3.version>
        <commons-io.version>2.17.0</commons-io.version>
        <guava.version>33.4.0-jre</guava.version>
        <libphonenumber.version>8.13.50</libphonenumber.version>

        <!-- Testing -->
        <testcontainers.version>1.20.4</testcontainers.version>
        <archunit.version>1.3.0</archunit.version>

        <!-- Internal module version -->
        <sanab.version>1.0.0-SNAPSHOT</sanab.version>
    </properties>

    <!-- ═══════════════════════════════════════════════════
         DEPENDENCY MANAGEMENT (BOM + version pinning)
         ═══════════════════════════════════════════════════ -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Modulith BOM -->
            <dependency>
                <groupId>org.springframework.modulith</groupId>
                <artifactId>spring-modulith-bom</artifactId>
                <version>${spring-modulith.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Testcontainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ─── Internal Modules ─── -->
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-shared</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-security</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-identity</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-catalog</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-inventory</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-customer</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-cart</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-orders</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-payments</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-shipping</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-returns</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-promotions</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-reviews</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-notifications</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-search</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-cms</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-support</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-analytics</artifactId>
                <version>${sanab.version}</version>
            </dependency>
            <dependency>
                <groupId>com.antigravity.sanab</groupId>
                <artifactId>sanab-admin</artifactId>
                <version>${sanab.version}</version>
            </dependency>

            <!-- ─── Security ─── -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>de.mkammerer</groupId>
                <artifactId>argon2-jvm</artifactId>
                <version>${argon2.version}</version>
            </dependency>

            <!-- ─── Mapping ─── -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <optional>true</optional>
            </dependency>

            <!-- ─── Communication ─── -->
            <dependency>
                <groupId>com.twilio.sdk</groupId>
                <artifactId>twilio</artifactId>
                <version>${twilio.version}</version>
            </dependency>

            <!-- ─── Utilities ─── -->
            <dependency>
                <groupId>org.springframework.retry</groupId>
                <artifactId>spring-retry</artifactId>
                <version>2.0.10</version>
            </dependency>
            <dependency>
                <groupId>org.thymeleaf.extras</groupId>
                <artifactId>thymeleaf-extras-java8time</artifactId>
                <version>3.0.4.RELEASE</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>

            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <dependency>
                <groupId>com.googlecode.libphonenumber</groupId>
                <artifactId>libphonenumber</artifactId>
                <version>${libphonenumber.version}</version>
            </dependency>

            <!-- ─── Testing ─── -->
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
                <scope>test</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- ═══════════════════════════════════════════════════
         COMMON DEPENDENCIES (inherited by all modules)
         ═══════════════════════════════════════════════════ -->
    <dependencies>
        <!-- Lombok — compile time only -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Testing — JUnit 5 + Mockito (all modules) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- ═══════════════════════════════════════════════════
         BUILD PLUGINS
         ═══════════════════════════════════════════════════ -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok-mapstruct-binding</artifactId>
                                <version>${lombok-mapstruct-binding.version}</version>
                            </path>
                        </annotationProcessorPaths>
                        <compilerArgs>
                            <arg>-parameters</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                            <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.flywaydb</groupId>
                    <artifactId>flyway-maven-plugin</artifactId>
                    <version>${flyway.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
